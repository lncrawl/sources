spec: 1
# Madara rather than plain WordPress: this deployment prints the rating beside each search result,
# which is what base/madara.yaml already describes.
extends: base/madara.yaml
base_url: https://daonovel.com/
disabled: The domain's nameservers answer SERVFAIL.
