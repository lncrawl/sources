spec: 1
base_url: https://csnovels.com/
disabled: "2026-08-12: a client-rendered app with an authenticated backend. No book link is in the served markup or in a render of the catalogue — 356 KB rendered, 36 links, all of them category filters — and the only configuration served is /runtime-config.js, which sets a SITE_SECRET the app sends to its API. Firebase storage is referenced. Would need that API reverse-engineered, so it is worth a decision before any more time."
