spec: 1
extends: base/fictioneer.yaml
base_url: https://wuxiasky.net/

novel:
  # Unlike the rest of the family, this deployment's `a.author` is a display field carrying the
  # original author rather than a link to a poster's archive.
  authors: { css: "a.author", all: true }
