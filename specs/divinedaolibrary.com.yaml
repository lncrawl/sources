spec: 1
extends: base/fictioneer.yaml
base_url: https://divinedaolibrary.com/

novel:
  # `a.author` here is the account that posted the translation, so the novel's own author is only
  # ever named in the summary text.
  authors: { regex: "Author\\s*:\\s*([^<\\n]{2,70})" }
